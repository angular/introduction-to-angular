// used to "shape" the data in TypeScript
export interface HousingLocation {
    name: string,
    city: string,
    state: string,
    photo: string,
    availableUnits: number,
    wifi: boolean,
    laundry: boolean
}
